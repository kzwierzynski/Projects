extends layout
block content
    h1 #{article.title}
    h4 Written by #{article.author}
    p #{article.body}
    br
    a.btn.btn-info(href="/articles/edit/" + article._id) Edit
    a.btn.btn-danger.del-art(href="#"+article._id data-id=article._id) Delete